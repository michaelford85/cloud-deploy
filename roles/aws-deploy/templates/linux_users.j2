{
{% for item in linux_users %}
{% if not loop.last %}
	"{{item.username}}_password": "{{ lookup('file', '{{ private_key_path }}') | b64encode }}",
{% else %}
	"{{item.username}}_password": "{{ lookup('file', '{{ private_key_path }}') | b64encode }}"
{% endif %}
{% endfor %}
}
